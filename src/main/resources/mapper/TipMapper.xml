<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ticket.ticketing.mapper.TipMapper">
    <select id="findCnt" resultType="Integer">
        SELECT COUNT(*) FROM tip_board
    </select>

    <select id="findAll">
        SELECT
            tip_id,
            title,
            content,
            created_at,
            deleted,
            ( SELECT nickname FROM `member` ) as nickname FROM tip_board
            ORDER BY created_at DESC
        LIMIT #{pageNumber}, 20
    </select>

    <select id="findAllByTitle">
        SELECT
            tip_id,
            title,
            content,
            created_at,
            deleted,
            ( SELECT nickname FROM `member` ) as nickname FROM tip_board
                WHERE title LIKE CONCAT('%', #{searchValue}, '%')
                ORDER BY created_at DESC
                LIMIT #{pageNumber}, 20
    </select>

    <select id="findAllByNickname">
        SELECT
            tip_id,
            title,
            content,
            created_at,
            deleted,
            ( SELECT nickname FROM `member` ) as nickname
        FROM tip_board
                WHERE ( SELECT nickname FROM `member` ) LIKE #{searchValue}
                ORDER BY created_at DESC
                LIMIT #{pageNumber}, 20
    </select>

    <insert id="upload">
        INSERT INTO tip_board
        (user_id, title, content, created_at, deleted)
        VALUES
            (
             #{tipDto.userId},
             #{tipDto.title},
             #{tipDto.content},
             now(),
             0
             )
    </insert>

</mapper>
