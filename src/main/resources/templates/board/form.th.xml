<?xml version="1.0"?>
<thlogic>
    <attr sel="#header" th:replace="header :: header" />
<!--    <attr sel="#footer" th:replace="footer :: footer" />-->

    <attr sel="#board-form-header/h1" th:text="${formStatus} ? '게시글 ' + ${formStatus.description} : _" />

<!--    <attr sel="#board-form" th:action="${formStatus?.update} ? '/board/tip/' + ${board.postId} + '/form' : '/board/tip/form'" th:method="post">-->
<!--        <attr sel="#title" th:value="${board?.title} ?: _" />-->
<!--        <attr sel="#content" th:text="${board?.content} ?: _" />-->
<!--        <attr sel="#submit-button" th:text="${formStatus?.description} ?: _" />-->
<!--        <attr sel="#cancel-button" th:onclick="'history.back()'" />-->
<!--    </attr>-->
</thlogic>